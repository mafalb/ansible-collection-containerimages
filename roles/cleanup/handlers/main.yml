---

# handler machen keine idempotenz probleme und laufen nur wenn sie muessen.
#
- name: Commit buildah container
  ansible.builtin.command:
    cmd: buildah commit {{ os }}-{{ role }} {{ image }}  # noqa: no-changed-when
  when:
    - os is defined
    - role is defined
    - image is defined
...
